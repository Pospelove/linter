## Codebase Patterns
- Use `override` keyword for all methods that override base class methods (required by `noImplicitOverride` in `tsconfig.json`).
- Use `AiProviderOptions` interface from `ai-providers/base-ai-provider.ts` for AI provider `call` methods.
- The `as` operator is strictly banned; use proper typing or `@ts-expect-error` if absolutely necessary.
- Avoid the string " as " (with spaces) in comments and strings as it triggers the `no-ts-as-operator` linter.
- Always run `yarn build` after code changes to keep `dist/linter.mjs` updated.
- Use local variables for private fields after null checks to satisfy strict TSC null checks.
- Explicitly declare all class properties, especially in base classes.
- Use inline type imports (e.g., `import("./path").Type`) for return types in base classes and interfaces to avoid circular dependencies.
- Access `process.env` properties via bracket notation (e.g., `process.env['KEY']`) to satisfy `noPropertyAccessFromIndexSignature`.
- Use `p.charAt(i)` instead of `p[i]` for string indexing when `noUncheckedIndexedAccess` is enabled to avoid `undefined` types.
- Always run `npm run build` after changing `linter.ts` or its dependencies.
- Ensure all files end with a newline to satisfy `linelint`.

## 2026-06-18 10:00 - US-001
- Fixed TypeScript errors in `ai-providers/base-ai-provider.ts`, `ai-providers/claude.ts`, and `ai-providers/echo.ts`.
- Installed `@types/node` to resolve Node.js built-in type errors.
- Added `AiProviderOptions` interface to `BaseAiProvider`.
- Added `override` modifiers to subclasses.
- Files changed: `ai-providers/base-ai-provider.ts`, `ai-providers/claude.ts`, `ai-providers/echo.ts`, `package.json`.
- **Learnings for future iterations:**
  - The codebase has `noImplicitOverride: true` in `tsconfig.json`, so `override` is mandatory.
  - Node.js types were missing and needed to be installed.
  - `static override` is accepted by the compiler (tested with TS 6.0.2).
---

## 2026-06-18 10:30 - US-002
- Fixed TypeScript errors in `ai-providers/gemini.ts`, `ai-providers/openai-compatible.ts`, and `checks/ai-prompt-check.ts`.
- Updated `checks/base-check.ts` to include proper property declarations and return types for better compatibility.
- Resolved "as" operator violations in `checks/ai-prompt-check.ts` by rephrasing strings and comments.
- Added missing `override` and `static override` modifiers.
- Properly typed options and parameters using `AiProviderOptions` and `CheckResult`.
- Files changed: `ai-providers/gemini.ts`, `ai-providers/openai-compatible.ts`, `checks/ai-prompt-check.ts`, `checks/base-check.ts`, `CLAUDE.md`.
- **Learnings for future iterations:**
  - The `no-ts-as-operator` linter is very aggressive and matches the string " as " even in comments and strings. Rephrase to "in format" or "like" to avoid this.
  - `BaseCheck` needed property declarations to allow subclasses to use `this.repoRoot` without TSC errors in strict mode.
  - Always import types like `CheckResult` when overriding methods in subclasses.
---

## 2026-06-18 11:00 - US-003
- Fixed TypeScript errors in `checks/always-fail-check.ts`, `checks/base-check.ts`, and `checks/check-utils.ts`.
- Updated `expanders/base-expander.ts` and `expanders/file-expander.ts` with proper types to resolve dependencies.
- Rephrased comments in `checks/base-check.ts` and `checks/check-utils.ts` to avoid aggressive `no-ts-as-operator` linter.
- Fixed `BaseCheck.expand` by using a local variable for the private `#expander` field to satisfy TSC null check.
- Fully typed `checks/check-utils.ts` functions and resolved `any`/`unknown` type issues.
- Files changed: `checks/always-fail-check.ts`, `checks/base-check.ts`, `checks/check-utils.ts`, `expanders/base-expander.ts`, `expanders/file-expander.ts`, `CLAUDE.md`, `dist/linter.mjs`.
- **Learnings for future iterations:**
  - TSC (at least in this project's configuration) sometimes fails to narrow the type of private fields even after a null check. Using a local variable for the private field before the check resolves this.
  - Base classes need explicit property declarations (e.g., `options: any`) to be correctly recognized as having those properties when accessed in the base class or subclasses.
---

## 2026-06-18 11:30 - US-004
- Fixed TypeScript errors in `checks/clang-format-check.ts`, `checks/codegen-check.ts`, and `checks/composite-check.ts`.
- Replaced `import { promises as fs }` with `import fs from "fs/promises"` to avoid `no-ts-as-operator` linter violations.
- Added explicit property declarations for private and protected fields.
- Prefixed unused parameters with `_`.
- Added `override` and `static override` modifiers.
- Resolved `unknown` error types in catch blocks.
- Files changed: `checks/clang-format-check.ts`, `checks/codegen-check.ts`, `checks/composite-check.ts`, `CLAUDE.md`.
- **Learnings for future iterations:**
  - `import { ... as ... }` is caught by the aggressive `no-ts-as-operator` linter. Use alternative import syntaxes.
  - Subclasses overriding `BaseCheck` methods should explicitly return `Promise<CheckResult>` (not `Promise<CheckResult | null>`) if they always return a result, to avoid needing type assertions in callers or overrides.
  - `static override` is valid and required for overriding static methods in this project.
  - `execFileAsync` (from `promisify(execFile)`) can have tricky overload issues in strict mode; `@ts-expect-error` is a valid escape hatch if idiomatic fix is too verbose.
---

## 2026-06-18 12:00 - US-005
- Fixed TypeScript errors in `checks/crlf-check.ts`, `checks/custom-check.ts`, and `checks/encoding-check.ts`.
- Rephrased comments and strings in `checks/custom-check.ts` and `checks/encoding-check.ts` to avoid `no-ts-as-operator` violations.
- Added explicit type declarations and `override` modifiers.
- Fixed `isAsciiOnly` in `checks/encoding-check.ts` to handle `undefined` when indexing into a Buffer.
- Properly typed `spawn` and `process.env` usage in `checks/custom-check.ts`.
- Files changed: `checks/crlf-check.ts`, `checks/custom-check.ts`, `checks/encoding-check.ts`, `CLAUDE.md`.
- **Learnings for future iterations:**
  - Even common phrases like "surfaced as the diagnostic output" trigger `no-ts-as-operator`. Rephrase to "surfaced for" or "surfaced like".
  - Buffer indexing can return `undefined` in this project's TSC config, so check before comparing.
  - `spawn`'s "close" event callback takes `number | null`.
---

## 2026-06-18 12:30 - US-006
- Fixed TypeScript errors in `checks/firecrawl-check.ts`, `checks/linelint-check.ts`, and `checks/localization-key-check.ts`.
- Replaced `import { promises as fs }` with `import fs from "fs/promises"`.
- Added explicit type declarations for all private fields.
- Fixed registry initialization in `LocalizationKeyCheck` by explicitly typing `#registeredKeys` as `Set<string> | null`.
- Resolved "possibly undefined" errors when accessing array elements after `split()`.
- Properly typed `RegExp.exec()` results and collection iterations.
- Files changed: `checks/firecrawl-check.ts`, `checks/linelint-check.ts`, `checks/localization-key-check.ts`, `CLAUDE.md`.
- **Learnings for future iterations:**
  - When initializing a field to `null` that will later hold a `Set` or `Map`, explicit typing (e.g., `Set<string> | null`) is required to avoid `never` or `unknown` type inference.
  - `RegExpExecArray` is the correct type for `RegExp.exec()` results.
  - Accessing `process.env` properties via index signature (e.g., `process.env['KEY']`) is sometimes required by strict linter rules.
---

## 2026-06-18 13:00 - US-007
- Fixed TypeScript errors in `checks/paired-files-check.ts`, `checks/regex-check.ts`, and `checks/tsc-check.ts`.
- Replaced `import { promises as fs }` with `import fs from "fs/promises"`.
- Added explicit property declarations and `override` modifiers.
- Resolved type mismatch between `null` and `undefined` in `TscCheck.resolveDeps`.
- Fixed unused parameter errors in `PairedFilesCheck`.
- Properly typed regex execution results and line offset calculations in `RegexCheck`.
- Files changed: `checks/paired-files-check.ts`, `checks/regex-check.ts`, `checks/tsc-check.ts`, `CLAUDE.md`.
- **Learnings for future iterations:**
  - Tools that return `null` (like `checkInPath`) may conflict with fields/returns typed as `undefined`. Use `?? undefined` to normalize.
  - RegExp exec matches in a loop should be typed as `RegExpExecArray | null`.
  - When a promise is cached in a private field (like `#resultPromise`), ensure it is correctly typed to match the expected return.
---

## 2026-06-18 13:30 - US-008
- Fixed TypeScript errors in `entries/base-entry.ts`, `entries/file-entry.ts`, and `entries/json-array-entry.ts`.
- Replaced `import { promises as fs }` with `import fs from "fs/promises"`.
- Rephrased multiple comments and strings to avoid `no-ts-as-operator` violations.
- Added explicit type declarations for all private fields and method parameters.
- Added `override` modifiers to all subclass methods and getters.
- Files changed: `entries/base-entry.ts`, `entries/file-entry.ts`, `entries/json-array-entry.ts`, `CLAUDE.md`.
- **Learnings for future iterations:**
  - Common descriptive phrases like "as a string" or "as JSON" trigger the linter. Use "like a string" or "in JSON format".
  - Getters in base classes (like `BaseEntry.id`) should be explicitly typed to enforce correct types in subclasses.
---

## 2026-06-18 14:00 - US-009
- Fixed TypeScript errors in `expanders/base-expander.ts` and `expanders/json-array-expander.ts`.
- Prefixed unused `file` parameter with `_` in `BaseExpander.expand`.
- Added `override` and `static override` modifiers in `JsonArrayExpander`.
- Explicitly typed `file: string` and added return type to `JsonArrayExpander.expand`.
- Updated `dist/linter.mjs` by running `npm run build`.
- Files changed: `expanders/base-expander.ts`, `expanders/json-array-expander.ts`, `dist/linter.mjs`, `ralph/progress.txt`.
- **Learnings for future iterations:**
  - Inline imports like `import("../entries/base-entry.js").BaseEntry[]` are preferred for types in method signatures to keep the import section clean and avoid cycles.
  - Even if a file is listed in a user story but passes the linter (like `expanders/file-expander.ts`), still verify its content against project patterns.
---

## 2026-06-18 14:30 - US-010
- Fixed TypeScript errors in `file-sources/all-files-source.ts`, `file-sources/base-file-source.ts`, and `file-sources/diff-base-source.ts`.
- Added explicit property declarations to `BaseFileSource`.
- Added `override` and `static override` modifiers to subclasses.
- Fixed `process.env` access by using bracket notation.
- Resolved "possibly undefined" error in `matchGlob` by using `p.charAt(i)`.
- Properly typed filter narrowing with `filePath is string`.
- Files changed: `file-sources/all-files-source.ts`, `file-sources/base-file-source.ts`, `file-sources/diff-base-source.ts`, `file-sources/CLAUDE.md`.
- **Learnings for future iterations:**
  - `BaseFileSource` (and other base classes) must declare properties like `repoRoot` and `options` to be accessible in subclasses without TSC errors.
  - TSC's `noPropertyAccessFromIndexSignature` requires `process.env['KEY']` instead of `process.env.KEY`.
  - Type narrowing in `Array.filter` works best with an explicit type predicate: `(item): item is Type => item !== null`.
  - `p.charAt(i)` is a safe alternative to `p[i]` for string indexing when `noUncheckedIndexedAccess` is enabled.
---

## 2026-06-18 15:00 - US-011
- Fixed TypeScript errors in `file-sources/staged-files-source.ts`, `linter.ts`, and `tool-resolve/clang-format.ts`.
- Added missing `override` and `static override` modifiers.
- Fully typed `linter.ts` (approx 1000 lines) and resolved numerous `any` and `unknown` type issues.
- Rephrased multiple strings and comments in `linter.ts` to avoid `no-ts-as-operator` violations.
- Fixed `checkVersion` in `clang-format.ts` to handle potential `undefined` match groups.
- Handled potential `undefined` in `process.argv` and CLI argument indexing.
- Files changed: `file-sources/staged-files-source.ts`, `linter.ts`, `tool-resolve/clang-format.ts`, `CLAUDE.md`, `dist/linter.mjs`.
- **Learnings for future iterations:**
  - `process.argv[1]` is not guaranteed to be defined in some environments; use a fallback.
  - `RegExp.match()` groups can be `undefined` even if the overall regex matches.
  - When iterating over a registry where different classes have slightly different `static` return types (e.g., `getHelp()`), use `@ts-expect-error` selectively for the specific loop that accesses the optional property.
  - Large files like `linter.ts` require careful typing of loop variables and Map/Set collections to avoid cascading `any` types.
---

## 2026-06-18 15:30 - US-012
- Fixed TypeScript errors in tool-resolve/linelint.ts, tool-resolve/tool-utils.ts, and util.ts.
- Added explicit types to all exported functions and methods.
- Resolved Promise constructor errors by using new Promise<void>().
- Handled unknown error types in catch blocks using instanceof Error or String(err).
- Updated CLAUDE.md with consolidated TypeScript patterns.
- Files changed: tool-resolve/linelint.ts, tool-resolve/tool-utils.ts, util.ts, CLAUDE.md, dist/linter.mjs.
- **Learnings for future iterations:**
  - When a Promise doesn't return a value, explicitly type it as Promise<void> or use new Promise<void>() to avoid TS2794.
  - TSC in strict mode types catch block errors as unknown; use err instanceof Error to safely access .message.
  - Exported functions should always have explicit return types to satisfy project standards and improve maintainability.
---

## 2026-06-18 16:00 - US-013
- Fixed codegen issue in `linter.ts` by updating `dist/linter.mjs` via `npm run build`.
- Resolved TSC error in `linter.ts` by adding `encoding: "utf-8"` to `spawnSync` call for `git add`.
- Resolved TSC errors in `checks/ai-prompt-check.ts` by allowing `undefined` for optional prompts and providing fallbacks.
- Removed unused `@ts-expect-error` in `tool-resolve/clang-format.ts`.
- Fixed `linelint` errors in `.gitignore`, `AGENTS.md`, `CLAUDE.md`, and `ralph/ralph.sh` by adding missing trailing newlines.
- Verified that all 202 checks pass with `npm run lint`.
- Files changed: `linter.ts`, `dist/linter.mjs`, `checks/ai-prompt-check.ts`, `tool-resolve/clang-format.ts`, `.gitignore`, `AGENTS.md`, `CLAUDE.md`, `ralph/ralph.sh`.
- **Learnings for future iterations:**
  - `npm run build` MUST be run after any change to `linter.ts` or its dependencies to keep `dist/linter.mjs` in sync and pass the `codegen` check.
  - `ensureCleanExit` expects `SpawnSyncReturns<string>`, so `spawnSync` calls must include `encoding: "utf-8"` if their result is passed to it.
  - Aggressive linting like `linelint` requires attention to small details like EOF newlines.
  - `tsc` might not catch everything if run only on specific files; periodic full project linting (`npm run lint`) is essential.
---

## 2026-06-18 16:30 - US-001
- Fixed TypeScript errors and banned 'any' type in checks/ai-prompt-check.ts, checks/always-fail-check.ts, and checks/base-check.ts.
- Updated BaseCheck to use Record<string, unknown> for options and dependencies.
- Added explicit type guards and narrowing to resolve 'as' operator violations.
- Handled exactOptionalPropertyTypes: true in AiPromptCheck by conditionally building options objects.
- Files changed: checks/ai-prompt-check.ts, checks/always-fail-check.ts, checks/base-check.ts, dist/linter.mjs, ralph/prd.json.
- **Learnings for future iterations:**
  - The 'as' operator is strictly banned, even for common casts like 'as string[]'. Use type guards with Array.isArray and filter(item: item is string) instead.
  - With 'exactOptionalPropertyTypes: true', you cannot pass { key: undefined } to a function expecting { key?: Type }. Use an intermediate object and only set the property if it is defined.
  - Base classes like BaseCheck should use Record<string, unknown> for flexible options instead of any.
---
