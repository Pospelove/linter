{
  "modes": { "manual": { "fileSource": { "export": "AllFilesSource" } } },
  "checks": [
    {
      "name": "botched-check",
      "export": "RegexCheck",
      "modes": ["manual"],
      "options": {
        "pattern": "foo.*",
        "message": "no foo"
      },
      "prd": { "storySplitMode": "per-finding" }
    }
  ]
}
