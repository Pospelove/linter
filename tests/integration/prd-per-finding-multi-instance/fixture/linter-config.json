{
  "toolsDir": "tools",
  "modes": {
    "manual": { "fileSource": { "export": "AllFilesSource" } }
  },
  "checks": [
    {
      "name": "todo-check",
      "export": "RegexCheck",
      "modes": ["manual"],
      "options": { "pattern": "TODO" },
      "prd": { "storySplitMode": "per-finding" }
    }
  ]
}
