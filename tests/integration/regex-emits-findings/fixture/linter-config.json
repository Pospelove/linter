{
  "modes": {
    "manual": { "fileSource": { "export": "AllFilesSource" } }
  },
  "checks": [
    {
      "name": "regex-hits",
      "export": "RegexCheck",
      "modes": ["manual"],
      "options": {
        "extensions": [".ts"],
        "pattern": "TODO|FIXME",
        "message": "Found a task."
      }
    }
  ]
}
